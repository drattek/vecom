**/.gradle
**/build
**/node_modules
**/dist
**/bin
**/vendor

# Secrets / credentials — never commit real values (see CLAUDE.md security note)
.env
.env.example
*.p12
*.pem
*.key

# Local-only tool state (machine-specific, not shared project config)
.claude/
.impeccable/config.local.json

# Compiled binaries
*.exe